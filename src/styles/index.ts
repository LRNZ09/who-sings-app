import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
	container: {
		flex: 1,
		maxWidth: 512,
		padding: 8,
	},
	fab: {
		bottom: 0,
		margin: 16,
		position: 'absolute',
		right: 0,
	},
	wrap: {
		flexWrap: 'wrap',
	},
})

export default styles
